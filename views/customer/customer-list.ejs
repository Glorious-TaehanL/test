
   <%- include('../header.ejs') %>
   <!-- Layout wrapper -->
   <div class="layout-wrapper layout-content-navbar">
     <div class="layout-container">
       <!-- Menu -->
       <%- include('../menu.ejs') %>
       <!-- /Menu -->

       <!-- Layout container -->
       <div class="layout-page">
        <!-- Navbar -->
        <%- include('../navbar.ejs') %>
        <!-- /Navbar -->

         <!-- Content wrapper -->
        <div class="content-wrapper">
          <section class="notice">
            <div class="page-title">
                  <div class="container">
                      <h3>학생관리</h3>
                  </div>
              </div>
            
            <!-- board list area -->
              <div id="board-list">
                  <div class="container">
                      <table class="board-table">
                          <thead>
                          <tr>
                              <th scope="col" class="th-num">번호</th>
                              <th scope="col" class="th-name">이름</th>
                              <th scope="col" class="th-email">이메일</th>
                              <th scope="col" class="th-contact">연락처</th>
                              <!-- <th scope="col" class="th-num">관리</th> -->
                          </tr>
                          </thead>
                          <tbody>
                            <% for(var i = 0; i < customers.length; i++){ %>
                                <tr>
                                    <td><%= customers[i].num %></td>
                                    <td><%= customers[i].name %></td>
                                    <td><%= customers[i].email %></td>
                                    <td><%= customers[i].phonenumber %></td>
                                    <!-- <td>
                                        <div id="removeCustomer" data-id="<%= customers[i].num %>">
                                            <i class="fa-solid fa-user-slash"></i>
                                        </div>
                                    </td> -->
                                </tr>
                            <% } %>
                          </tbody>
                      </table>
                  </div>
              </div>
          </section>
         <!-- Content wrapper -->
       </div>
       <!-- / Layout page -->
     </div>
     <!-- <script>
         const myDiv = document.getElementById('removeCustomer');

            myDiv.addEventListener('click', function() {
            const result = confirm('정말로 삭제하시겠습니까?');

            if (result) {
                // myDiv.remove();
                console.log('요소가 삭제되었습니다.');
            } else {
                console.log('요소 삭제가 취소되었습니다.');
            }
            });
     </script> -->
     <!-- Overlay -->
     <div class="layout-overlay layout-menu-toggle"></div>
   </div>
   <%- include('../footer.ejs') %>
