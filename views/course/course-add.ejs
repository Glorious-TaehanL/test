
   <%- include('../header.ejs') %>
   <!-- Layout wrapper -->
   <div class="layout-wrapper layout-content-navbar">
     <div class="layout-container">
       <!-- Menu -->
       <%- include('../menu.ejs') %>
       <!-- /Menu -->

       <!-- Layout container -->
       <div class="layout-page">
        <!-- Navbar -->
        <%- include('../navbar.ejs') %>
        <!-- /Navbar -->

         <!-- Content wrapper -->
        <div class="content-wrapper">
          <div class="container">
            <h4>강의 등록</h4>
            <form method="POST" action="/course/add" enctype="multipart/form-data">
                <div class="container course-preview">
                    <label>강의 미리보기 이미지 등록</label>
                    <div id="image_container" style="width: 250px; height: 250px; border: 1px solid; background: no-repeat; background-size: contain; 	background-position: center center;"></div>
                    <label class="btn btn-outline-secondary" for="thumnail_course">
                        업로드
                      </label>
                    <input id="thumnail_course" name="thumnail_course" type="file" onchange="setThumbnail(event)" style="display:none;" />
                </div>
    
                <div class="container course-information">
                  <p>강의 제목</p>
                  <input id="course_title" name="course_title" type="text" size="60" />
                  <p>강의 가격</p>
                  <input id="course_price" name="course_price" type="number" size="60" />원
                  <p>총강의 주차</p>
                  <input id="course_term" name="course_term" type="number" size="60" />
                  <p>강사</p>
                  <input id="course_teacher" name="course_teacher" type="text" size="60" />
                  <p>강의 소개</p>
                  <textarea name="course_description" cols="100" rows="10"></textarea>
                  <label>강의 소개</label>
                  <p class="hidden-data" style="display: none;"></p>
                  <textarea id="summernote"  name="course_information" ></textarea>
                </div>
                
                <button type="submit" class="btn btn-outline-secondary">Submit</button>
            </form>
          </div>
          <script src="/assets/js/summernote.js"></script>
        <script>
            function setThumbnail(event){
                var reader = new FileReader();
                reader.onload = function(event) {
                    document.getElementById("image_container").style.backgroundImage= `url(${event.target.result})`;
                };
                reader.readAsDataURL(event.target.files[0]);
            }
        </script>
         <!-- Content wrapper -->
       </div>
       <!-- / Layout page -->
     </div>

     <!-- Overlay -->
     <div class="layout-overlay layout-menu-toggle"></div>
   </div>
   <%- include('../footer.ejs') %>

      
    